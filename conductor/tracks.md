# Project Tracks

This file tracks all major tracks for the project. Each track has its own detailed plan in its respective folder.

- [x] `database_schema_alignment` - Adapt MariaDB schema to support full project overview (movements, invoices, payments, and receipts).
- [~] `app_refactoring_worksheet_sync` - Refactor PHP models and controllers to match the consolidated worksheet-based schema.

